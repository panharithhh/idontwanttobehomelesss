import numpy as np
from numpy._core.defchararray import splitlines
from numpy.strings import count 

class minbpe():

    def __init__(self) -> None:
        pass 


    def train(self, text, vocab_size, verbose=False)
        tokens = open(text,'r').read()
        tokens = tokens.encode("utf-8") # raw bytes 
        
        


        # Identify till (,)
        counts = {} # a for loop that will add up all of the stuff (222) 
        
        for 
        
        
        top_pair = max(counts, key = counts.get)
        
        pass

    def encode():    
        pass


    def decode():
        pass

    

